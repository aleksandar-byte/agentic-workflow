interface:
  display_name: "Agentic Brand Review"
  short_description: "Read-only brand and copy review"
  default_prompt: "Use $agentic-review-brand to review the requested change and return evidence-backed findings without modifying anything."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Agentic Security Review"
  short_description: "Read-only security change review"
  default_prompt: "Use $agentic-review-security to review the requested change and return evidence-backed findings without modifying anything."
policy:
  allow_implicit_invocation: false

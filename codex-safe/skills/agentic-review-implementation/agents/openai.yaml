interface:
  display_name: "Agentic Implementation Review"
  short_description: "Read-only findings synthesis and routing"
  default_prompt: "Use $agentic-review-implementation to review the requested change and return evidence-backed findings without modifying anything."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Agentic Code Review"
  short_description: "Read-only correctness and code review"
  default_prompt: "Use $agentic-review-code to review the requested change and return evidence-backed findings without modifying anything."
policy:
  allow_implicit_invocation: false

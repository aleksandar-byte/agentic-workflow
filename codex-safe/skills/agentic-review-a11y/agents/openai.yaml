interface:
  display_name: "Agentic Accessibility Review"
  short_description: "Read-only accessibility change review"
  default_prompt: "Use $agentic-review-a11y to review the requested change and return evidence-backed findings without modifying anything."
policy:
  allow_implicit_invocation: false

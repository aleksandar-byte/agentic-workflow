interface:
  display_name: "Agentic Design Review"
  short_description: "Read-only UI and UX design review"
  default_prompt: "Use $agentic-review-design to review the requested change and return evidence-backed findings without modifying anything."
policy:
  allow_implicit_invocation: false

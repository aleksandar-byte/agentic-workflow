# planning-findings — fix-214 (model-selection-over-24-options)

One stage-aware table; rows are appended by reviewers and resolved only by the
stage's author through its own route. Shape:
`finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision`.

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PL-1 | plan | low | plan | c7b0461dfd4f21912efaf238d0a570ed74a941cf9da5746928bec86f4e79f4d9 | PE-007's `source-and-location` anchors do not resolve at the cited lines: the test-facing-contract comment sits at `console.ts:45-47` (cited `:62-66`), the `prompts` table spans `:48-80` with `} as const;` at `:80` (cited `:76-99`), and `scriptedUi` spans `test/settings-console.test.mjs:26-60` (cited `:16-20`, which is the import block) | read of `packages/pi-agentic-workflow/src/settings/console.ts:45-80` and `test/settings-console.test.mjs:14-60` at sourceRevision af8d6431; `grep -n` pins: "The console's questions" comment → `:45-47`, `export const prompts` → `:48`, `command:` → `:54`, `} as const;` → `:80`, `function scriptedUi` → `:28` | resolved | PE-007's anchors corrected in SPEC.md (repair ar-214-2): comment `console.ts:45-47`, `prompts` table `:48-80` (`} as const;` at `:80`), `scriptedUi` `test/settings-console.test.mjs:26-60`. Every other PE-row anchor re-verified against the source at e0c18284 in the same pass — PE-002 (`extension/index.ts:60-65`), PE-003 (`:269`, guard `:261`, TYPED `:84`), PE-004 (`:366-372`/`:371`, `:375-398`/`:391`), PE-005 (`:261-268`, `picker.ts:67-107`, `extension/index.ts:66-90`) all resolve. | ar-214-2 |
| PL-2 | plan | medium | plan | c7b0461dfd4f21912efaf238d0a570ed74a941cf9da5746928bec86f4e79f4d9 | The scenario matrix never exercises the pick-less over-cap model path: O1 claims "> 23 model lists select provider-first in two bounded steps in every UI mode" and PE-003 names the select fallback (`console.ts:269`) as root-cause surface #2, but every frozen model-picker test name runs the rich fixture (`scriptedUi` defines `pick`, `test/settings-console.test.mjs:53`), so a two-step implemented only inside the rich branch passes all frozen validators while `console.ts:269` stays unbounded | SPEC `## Testing` + P2 task 1 + O1 vs `test/settings-console.test.mjs:53` (pick fixture) and `:263-299` — the harness already supports a pick-less UI (`rich: false`, used by the "P4/OB-12" test at `:264`), but no frozen case pairs it with an over-cap list | resolved | Red-first pick-less over-cap case added (repair ar-214-2): frozen test name "settings console model picker: provider-first two-step over 30 models in a pick-less UI" added to `## Testing`; its own red-first task in P2 (drives `rich: false` through the select fallback `console.ts:269`, every dialog ≤ 24 options, TYPED last); O1 re-pointed to P2 task 3 with validator `-t "provider-first"` → 2 pass; AC2 validator strengthened to 2 pass; PE-013 added (harness capability at `test:26-60` + `:266-286`, unbounded fallback branch `console.ts:268-270`); O2-O4 task pointers 2→3 kept the obligation→task mapping exact; P2 re-linted 8/8, fingerprint rotated to `P2:domain:3:provider-first-two-step-model-selection`. | ar-214-2 |
| PL-3 | plan | medium | plan | 5ef96fc8be539a225e92627f23ee7371bb967ce2fdeae78c62eccca9c660fb6f | Decision 2 closes the provider-count gap via provider-list paging (P2 task 3 asks the provider "via `pick`/`pagedSelect`"), but no frozen scenario forces the provider dialog over 24 options: the two-step test names ("provider-first two-step over 30 models" and its pick-less variant) never pin the fixture's provider distribution, so an implementation asking the provider with plain `pick`/`select` passes every frozen validator (AC2-AC6) while a registry with > 24 providers crashes the first dialog — the same unexercised-branch shape PL-2 fixed for the mode split, now on the paging split | SPEC `## Decisions made during drafting` 2 + `### P2` task 3 vs `## Testing` names and ACCEPTANCE AC2-AC6: no `-t` case or fixture description names > 24 providers; O2 pins only within-provider model paging ("pages within one provider over 30 models"); read at f1a4f4b2 | resolved | Dedicated over-cap provider-list case added (repair ar-214-3): frozen test name "settings console model picker: provider-first two-step over 30 providers" (30 providers × 1 model each — the provider dialog itself exceeds the cap and pages, `More options…` reaches the page-2 providers, every dialog ≤ 24) added to `## Testing` with its own red-first task in P2 (inserted as task 3, implement renumbered to task 4); O15 added (provider dialog pages when the unique provider list exceeds the cap — Decision 2's paging now has an obligation row) with validator `-t "over 30 providers"`; O1's validator strengthened to 3 pass, AC2's validator with it, and the new command added to ACCEPTANCE Commands; PE-014 added (required failure state: Pi imposes no provider ceiling — the issue's "providers siempre ≤ 24" is an assumption, not an invariant); scope bullet now names the paged provider dialog; P2 re-linted 8/8, fingerprint rotated to `P2:domain:4:provider-first-two-step-model-selection`. | ar-214-3 |
| PL-4 | plan | low | plan | 5ef96fc8be539a225e92627f23ee7371bb967ce2fdeae78c62eccca9c660fb6f | AC12/O13's validator cannot verify the `done · [PR]` flip its required outcome names: `grep -c "model-selection-over-24-options" docs/fix/README.md → 1` is already green at plan time (row status `pending`), so the flip could be skipped while every frozen validator passes | ACCEPTANCE AC12 + obligations O13 vs measurement at f1a4f4b2: `grep -c "model-selection-over-24-options" docs/fix/README.md` → 1 (docs/fix/README.md:17, status `pending`); the repo's own stale-index history (fixes #100, #162) makes the flip a real failure category; backstops exist (turn contract box 4, audit-pr closure integrity) but the unit's own frozen validator is blind to the outcome it names | resolved | AC12/O13 validator strengthened to match the flip (repair ar-214-3): outcome reworded to "carries the #214 unit row and is flipped to `done · [PR]` at close-out"; validator now `grep -cE "model-selection-over-24-options.*done · \[#"` → 1 from repo root (blindness re-verified before the repair: plain `grep -c` → 1 with the row `pending` at docs/fix/README.md:17; flip format per the #157 row, `done · [#158](<pr-url>)`); O13 re-pointed to P7 task 6 — the task that writes `done · [#<pr>](<pr-url>)` — so the validator can only run after the flip exists; ACCEPTANCE Commands updated. | ar-214-3 |
| PL-5 | plan | low | plan | 5ef96fc8be539a225e92627f23ee7371bb967ce2fdeae78c62eccca9c660fb6f | The `## Operational risks` prose anchor is wrong: it cites `console.ts:101-116` as "the same parse gates", but those lines are the menu-select block (`deps.ui.select(prompts.menu, …)` at :103); the parse gates the sentence means are the invalid-save/invalid-draft refusals (`console.ts:216`, `:427`) — same genus as PL-1, prose anchor this time | `sed -n '95,120p' packages/pi-agentic-workflow/src/settings/console.ts` at f1a4f4b2 (menu block occupies 101-116); `grep -n` pins: invalid-save notify :216, invalid-draft notify :427 | resolved | Operational-risks anchor corrected (repair ar-214-3): the parse gates now cite `console.ts:216` (invalid-file save refusal — "Nothing can be saved over an invalid ${scope} file") and `:427` (invalid-draft save refusal — "Nothing saved — the draft is invalid"), both re-verified by sed at 2fbcedf0; the menu-select citation (`:101-116`) removed. Package source is unchanged since e0c18284 (every commit since is docs-only), so the correction re-verifies the same bytes. | ar-214-3 |
| PL-6 | plan | info | plan | 59423e3f199b421c65c06aa2550b75ce0c76b3abe486f816c816107e69851426 | The `## Rules that must never be violated` prose anchor for the parse gate cites `console.ts:288-297`, but the console-side `parseModelReference` enforcement (call, rejection block, success normalization) spans `:278-289`; the cited range lands on the function's final two lines (success return `:288`, close `:289`) and the next function's docstring — same genus as PL-1/PL-5, but the content is adjacent and the normative instruction unambiguous, so info (non-blocking) rather than low | `grep -n` pins at 6e6d3d78: `parseModelReference(value)` → :278, `Rejected: model must be` → :283, success return `` `${parts.provider}/${parts.id}` `` → :288, fn close → :289; sed 288-297 shows :291+ is the next function's docstring; the validation definition itself lives in `src/config/schema.ts` (imported at console.ts:14) | open | — | — |

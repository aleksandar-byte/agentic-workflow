# review-findings — fix-214 (model-selection-over-24-options)

Fix-now fold ledger for this unit, written by `/review-change` (Cycle 1,
2026-09-13, reviewed head `c1f89f79da8bf6ea861654da4685866f5691b806`, PR #217).
Schema: `| id | file:line | axis | severity | class | route | folded |` — each
row is followed by its `finding-mark@1` verification row. `folded` is flipped
only by the fold cycle (`/fold-findings`). Low findings are report-only notes
in the review report, never rows here.

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F1 | packages/pi-agentic-workflow/src/settings/console.ts:299-302 | code | med | fix-now | fold | yes |
| VF-1 | console.ts:299-302 · reviewer review-change · HEAD c1f89f79da8bf6ea861654da4685866f5691b806 · recheck direct read of all three sites — `providerOf` (console.ts:299-302, `indexOf("/")`/`slice`), `parseModelReference` (src/config/schema.ts:32 docstring + :39-43 split, stricter: validates/rejects, not a drop-in for the lenient split), inline `ref.split("/")[0]` (picker.ts:119); split semantics identical for every input, concept triplicated | code | confirmed | finding-mark | n/a | n/a |
| F2 | packages/pi-agentic-workflow/src/settings/picker.ts:49-63 · :84-92 | code + api-ergonomics | med | fix-now | fold | yes |
| VF-2 | picker.ts:49-63 · :84-92 · reviewer review-change · HEAD c1f89f79da8bf6ea861654da4685866f5691b806 · recheck failing reproducer (bun script in /tmp importing src/settings/picker.ts, 44 options → 3 pages): data option `"More options…"` answered on page 0 is intercepted by the navigation guard (page 1 shown, value never returned); last page returns it (`page < pageCount - 1` false) — JSDoc :57-58 "data entries are returned to the caller" violated on non-last pages; reservation undocumented | code + api-ergonomics | confirmed | finding-mark | n/a | n/a |
| F3 | CHANGELOG.md:95 · CHANGELOG.es.md:97 | brand | med | fix-now | fold | yes |
| VF-3 | CHANGELOG.md:95 · CHANGELOG.es.md:97 · reviewer review-change · HEAD c1f89f79da8bf6ea861654da4685866f5691b806 · recheck direct read: EN "Lists at or below the cap keep the previous single-dialog behavior byte-identical" / ES "Las listas en el tope o por debajo mantienen byte a byte el comportamiento de un solo diálogo" vs console.ts:264-267 routing `models.length > SELECT_OPTION_LIMIT - 1` (24 models included) to the two-step — false at the exact boundary (unit Decision 1) | brand | confirmed | finding-mark | n/a | n/a |
| F5 | packages/pi-agentic-workflow/src/settings/picker.ts:56 | api-ergonomics | med | fix-now | fold | yes |
| VF-5 | picker.ts:56 · reviewer review-change · HEAD c1f89f79da8bf6ea861654da4685866f5691b806 · recheck direct read: JSDoc "pages 21 data entries at a time" vs :44 `PAGED_SELECT_PAGE_SIZE = SELECT_OPTION_LIMIT - 3` and :28-30 constant JSDoc "the single knob if a future host enforces a different bound" — a sanctioned knob edit silently falsifies the hardcoded count (sibling comment :41-42 `21 + 2 + 1 = 24`) | api-ergonomics | confirmed | finding-mark | n/a | n/a |
| F6 | packages/pi-agentic-workflow/src/settings/picker.ts:40-44 | code | med | fix-now | fold | yes |
| VF-6 | picker.ts:40-44 · reviewer review-change · HEAD 6a778be225c557b404fadc43c6b6352a7460705f · recheck direct read: the `PAGED_SELECT_PAGE_SIZE` JSDoc still hardcodes the derived count ("the cap leaves room for both pager entries and the caller's trailing option (`21 + 2 + 1 = 24`)") while the constant derives it from `SELECT_OPTION_LIMIT - 3` and the knob's own JSDoc (:27-33) advertises "the single knob if a future host enforces a different bound" — with the knob at 30 the page is 27 (+2 pagers +1 trailing = 30) and the comment is stale. Sibling site of F5's hazard: VF-5 cited :41-42 as its corroboration, so the fold closed the cited line and left the adjacent copy | code | confirmed | finding-mark | n/a | n/a |
| F7 | packages/pi-agentic-workflow/test/settings-console.test.mjs:1008-1286 | verify | med | fix-now | fold | yes |
| VF-7 | test/settings-console.test.mjs:1008-1286 · reviewer review-change · HEAD 76f95712a002bc9bb2baf77e6b6c51102611e47d · recheck grep + direct read (missing-coverage method): `initial` appears only at :263 and :1175 (both fits-cap flows); every over-cap test (~:1008-1286) seeds no current model and asserts dialog options/kind/fitsCap only — no provider-dialog `opts.initial === currentProvider`, no within-provider initial positive/other-provider-negative; the only TYPED answer (:507-508) is a single-page 2-model registry, no test answers TYPED from a paged dialog. Claimed behaviors console.ts:310-312 (provider preselected to the value in force), :337-339/:370-372 (initial only when current belongs to the provider; TYPED → input fallback) confirmed CORRECT by a /tmp reproducer against the real build (provider preselect true, no cross-provider initial leak, within-provider preselect true, TYPED-from-paged reached the text input, flows saved) — coverage-only gap: a regression there would pass the whole AC1–AC13 suite | verify | confirmed | finding-mark | n/a | n/a |
| F8 | packages/pi-agentic-workflow/src/settings/picker.ts:177-207 · src/extension/index.ts:62-95 | design | med | fix-now | fold | yes |
| VF-8 | picker.ts:177-207 · extension/index.ts:62-95 · reviewer review-change · HEAD 3fb662f8bd6b0774e6dc8620199138e457c47f5a · recheck failing reproducer against real bytes (bun, /tmp, sha-verified picker.ts copy, installed pi-tui 0.85.1): rendered `createPickerComponent` opens on items[0] (`→ model-0`) with the value in force at index 3; adding `initial: "model-3"` to the options object changes nothing (factory source contains no `initial` / `setSelectedIndex`); raw `new SelectList(…)` starts `selectedIndex` 0 while `list.setSelectedIndex(3)` works — the setter exists in the installed dependency and is never called by the repo. TUI `pick` branch reads `opts.multiple` only (extension/index.ts:72, :95; grep `initial` in index.ts/picker.ts → 0 hits); contract routing/types.ts:49-50 ("preselected when the picker opens (OB-3)") + `SettingsUi.pick` doc; call sites console.ts:272, :342-343, :364-366, :435-436 pass `initial`; harness tests assert recorded `opts` only (settings-console.test.mjs:54-61, :263, :1175, :1314-1346; grep createPickerComponent/setSelectedIndex/getSelectedItem in test/ → 0 hits). Pre-existing since #154 for the single-step dialog; this unit extended the dead contract to its new two-step dialogs (SPEC Decision 7) | design | confirmed | finding-mark | n/a | n/a |
| VF-8-r | picker.ts:181-207 · extension/index.ts:85-91 · fold, 2026-09-14 (frozen class `fix-now`, route `fold`; repaired on branch fix/214-model-selection-over-24-options) · reproducer materialized from VF-8's `recheck` cell and observed RED before the fix: new `test/picker-component.test.mjs` (4 cases — Enter accepts the value in force, the selected row renders as `→ model-03`, an out-of-list initial keeps item 0, no initial keeps item 0, and narrowing after open keeps the narrowed window's own first match) plus 3 TUI cases in `test/shipped-adapter.test.mjs` driven through the real `richUi` + `ctx.ui.custom` seam; pre-fix run: 1 fail per file (`actual: model-01` where the value in force was `model-03`). Root cause fixed at both named sites: `createPickerComponent` takes `initial` and applies it to the first window via the installed `SelectList.setSelectedIndex` (index 0 is already its default), and `richUi`'s TUI branch forwards `opts.initial`. Preselection stays an open-time concern, so typing keeps the narrowed window's first match. Non-vacuous by /tmp mutation (repo never mutated): re-applying the initial on every rebuild turns the open-time case red (3 pass / 1 fail); dropping the adapter forwarding turns the adapter case red (2 pass / 1 fail). Gate: `cd packages/pi-agentic-workflow && bun run test` → exit 0, 214 pass / 0 fail (was 207). Single production caller of the component (`extension/index.ts:85`) — the whole blast radius | design | resolved | finding-mark | n/a | n/a |
| F9 | docs/fix/214-model-selection-over-24-options/ACCEPTANCE.md (## Commands — AC2–AC8 `-t` validators) · packages/pi-agentic-workflow/package.json:48 | verify | med | fix-now | replan-in-unit | yes |
| VF-9 | ACCEPTANCE.md ## Commands · package.json:48 · reviewer review-change · HEAD 3fb662f8bd6b0774e6dc8620199138e457c47f5a · recheck reproducer (/tmp copy, repo untouched): test suites import `../dist/*.js` (settings-console.test.mjs:15-20) and `"test": "tsc && bun test test/*.test.mjs"` (package.json:48) means a standalone `bun test -t` validator compiles nothing; a src mutation (picker.ts page size `- 3` → `- 1`) passed all 8 AC6 tests pre-tsc and failed 5 after `bun run test` rebuilt dist — a standalone validator can PASS on stale dist. ACCEPTANCE.md is frozen (blob 94fc0ec5) — repair is a user-approved amendment: note-only or tsc-first validator commands (decision required from the user) | verify | confirmed | finding-mark | n/a | n/a |
| VF-9-r | ACCEPTANCE.md ## Commands + AC2–AC8 · SPEC.md O1–O7/O15 validator cells · replan fold, user-approved 2026-09-14 · repair: tsc-first validators — the standalone `bun test … -t` commands compiled nothing (suites import `../dist/*.js`; only `tsc` rebuilds dist), reproduced on a /tmp copy (page-size mutation passed 8/8 pre-tsc, failed 5 after rebuild); all 15 ACCEPTANCE + 8 SPEC validator commands now `bun run build && bun test …` with test names, files and expected counts unchanged, amendment note added to the manifest header; blob rotated 94fc0ec5fc49ca4415dfa695615ef2c016a5ad22 → 4ea24ed70be9c40090202eb6a35fefc91d1fea8d; gate `bun run test` exit 0 (207 pass / 0 fail) and all 8 amended validators green (3/1/1/2/2/8/2/1 pass); package.json:48 script untouched (already correct) | verify | resolved | finding-mark | n/a | n/a |
| REVIEW-RAN | HEAD c1f89f79da8bf6ea861654da4685866f5691b806 | n/a | n/a | review-mark | n/a | n/a |
| REVIEW-RAN | HEAD 6a778be225c557b404fadc43c6b6352a7460705f | n/a | n/a | review-mark | n/a | n/a |
| REVIEW-RAN | HEAD 76f95712a002bc9bb2baf77e6b6c51102611e47d | n/a | n/a | review-mark | n/a | n/a |
| REVIEW-RAN | HEAD 3fb662f8bd6b0774e6dc8620199138e457c47f5a | n/a | n/a | review-mark | n/a | n/a |
